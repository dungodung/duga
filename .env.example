# Copy to .env for local development. Never commit the real .env file.

FLASK_ENV=development
SECRET_KEY=change-me-dev-only

# Mandatory descriptive User-Agent for any future outbound Wikimedia API/WDQS
# calls (M1+ scope_fetch/topic_refresh jobs). Not used yet in M0.
DUGA_USER_AGENT="Duga/0.1 (https://duga.toolforge.org; maintainer contact via meta.wikimedia.org) requests"
